class ExecutionRuntimeDispatchError(ValueError):
    """
    Raised when an execution runtime dispatch is invalid, unknown, or
    a dispatch, cancel, or status operation cannot be performed.
    """

    pass
